import { AlertDirective } from './alert.component';

describe('AlertDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertDirective();
    expect(directive).toBeTruthy();
  });
});
